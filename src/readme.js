export function handlereadmeClick(event) {
    const modal = document.querySelector(".readme-modal")
    modal.style.display = "block";
  }
  
export function handlereadmeClose(event) {
    const modal = document.querySelector(".readme-modal")
    modal.style.display = "none";
}
